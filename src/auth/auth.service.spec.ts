// auth.service.ts has been removed; auth is now handled by better-auth directly.
// This file is intentionally minimal.

describe('auth', () => {
  it('better-auth handles authentication', () => {
    expect(true).toBe(true);
  });
});
